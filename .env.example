# --- Fundamentals (Finnhub free tier, optional) ---
FINNHUB_API_KEY=               # optional: https://finnhub.io ; enables CANSLIM (EPS/sales growth).
                               # Without it, price-only screening still runs; CANSLIM marked unavailable.

# Prices via yahoo-finance2 (no key).
# No LLM key needed — this is a skill; the host agent supplies reasoning + web search.
